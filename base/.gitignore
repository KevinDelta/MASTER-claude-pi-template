# agentmemory binary store — regenerable from observation history
memory/.agentmemory/
memory/.agentmemory-export.json

# OpenClaw/project environment overrides
.env
openclaw/.env

# OpenClaw local session/cache material if copied into a project
.openclaw/
