{
  "_comment": "Permission config for pi-permission-system (github.com/MasuRii/pi-permission-system). Install first: pi install git:github.com/MasuRii/pi-permission-system. Rules are evaluated top-to-bottom; last matching rule wins. Policies: allow (no prompt), ask (prompt user), deny (block with message).",

  "defaultPolicy": "ask",

  "rules": {
    "bash": [
      {
        "_comment": "Outright destructive — always deny",
        "pattern": "rm -rf *",
        "policy": "deny",
        "reason": "Recursive delete blocked. Use 'rm' with explicit paths."
      },
      {
        "pattern": "git push --force*",
        "policy": "deny",
        "reason": "Force push blocked. Use --force-with-lease if absolutely necessary."
      },
      {
        "pattern": "git reset --hard*",
        "policy": "deny",
        "reason": "Hard reset blocks are load-bearing — confirm manually."
      },
      {
        "pattern": "git clean -f*",
        "policy": "deny",
        "reason": "Untracked file deletion requires explicit approval."
      },
      {
        "_comment": "Sensitive file reads — always deny",
        "pattern": "cat *.env*",
        "policy": "deny",
        "reason": "Env file reads blocked. Access secrets via env vars only."
      },
      {
        "pattern": "cat *auth.json*",
        "policy": "deny",
        "reason": "Auth file reads blocked."
      },
      {
        "pattern": "cat *secrets*",
        "policy": "deny",
        "reason": "Secrets file reads blocked."
      },
      {
        "_comment": "Safe read-only operations — allow without prompt",
        "pattern": "git status",
        "policy": "allow"
      },
      {
        "pattern": "git diff*",
        "policy": "allow"
      },
      {
        "pattern": "git log*",
        "policy": "allow"
      },
      {
        "pattern": "ls*",
        "policy": "allow"
      },
      {
        "pattern": "cat *.md",
        "policy": "allow"
      },
      {
        "pattern": "cat *.json",
        "policy": "allow"
      }
    ],

    "write": [
      {
        "_comment": "Block writes outside designated workspace folders",
        "pattern": "*/.env*",
        "policy": "deny",
        "reason": "Writing to env files blocked. Update via system environment only."
      },
      {
        "pattern": "*/auth.json",
        "policy": "deny",
        "reason": "Writing to auth files blocked."
      }
    ],

    "edit": [
      {
        "_comment": "Same restrictions as write for sensitive files",
        "pattern": "*/.env*",
        "policy": "deny",
        "reason": "Editing env files blocked."
      }
    ]
  }
}
